typedef struct no tipoNo;
struct no {
    int val;
    tipoNo* prox;
};

typedef struct {
    tipoNo* prim;
} tipoLista;

tipoNo* criaNo(int val, tipoNo* prox) {
    tipoNo* novo = malloc(sizeof(tipoNo));
    novo->val = val;
    novo->prox = prox;
    return novo;

    void iniciaLista(tipoLista* lista) {
        lista->prim = NULL;
    }

void insereNums(tipoLista* lista) {
    int num;
    for (int i;i<N;i++){
        num=vetor[i];

    lista->prim = criaNo(num, lista->prim);

    insereNums(lista);

    }
    }
